{
  "name": "opal-tools-registry",
  "version": "1.0.0",
  "description": "Simple Opal Tools Registry for Vercel",
  "scripts": {
    "dev": "dev",
    "build": "build",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
